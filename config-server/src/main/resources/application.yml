server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/markkanthony/greeting-config

logging:
  level:
    root: WARN
    com:
      orangeandbronze: DEBUG
